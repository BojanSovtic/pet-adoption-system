# feature-pets

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-pets` to build the library.

## Running unit tests

Run `nx test feature-pets` to execute the unit tests via [Jest](https://jestjs.io).
