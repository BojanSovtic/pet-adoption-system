# feature-adoption

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build feature-adoption` to build the library.

## Running unit tests

Run `nx test feature-adoption` to execute the unit tests via [Jest](https://jestjs.io).
